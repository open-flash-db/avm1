label_p0:
  defineFunction(name="f", parameters=[]) {
    label_p9:
      push(0="err");
      trace();
      push(0="err-value");
      throw;
  };
  push(0="start");
  trace();
  try {
    label_p56:
      push(0=0i32, 1="f");
      callFunction();
      pop();
      next label_p69;
  } catch(i:"e") {
  }
label_p69:
  push(0="end");
  trace();
  end;
