label_p0:
  defineFunction(name="f", parameters=[]) {
    label_p9:
      push(0="ok");
      trace();
    label_p17:
      end;
  };
  push(0="start");
  trace();
  try(catchTarget=i:"") {
    label_p39:
      push(0=0f64, 1="f");
      callFunction();
      pop();
      push(0="in-try");
      trace();
    label_p68:
      end;
  } finally {
    label_p68:
      push(0="in-finally");
      trace();
    label_p84:
      end;
  };
  push(0="end");
  trace();
label_p93:
  end;
