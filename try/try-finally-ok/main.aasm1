label_p0:
  defineFunction(name="f", parameters=[]) {
    label_p9:
      push(0="ok");
      trace();
      end;
  };
  push(0="start");
  trace();
  try {
    label_p39:
      push(0=0f64, 1="f");
      callFunction();
      pop();
      push(0="in-try");
      trace();
      next label_p68;
  } finally {
    label_p68:
      push(0="in-finally");
      trace();
      next label_p84;
  }
label_p84:
  push(0="end");
  trace();
  end;
