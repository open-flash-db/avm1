label_p0:
  defineFunction(name="f", parameters=[]) {
    label_p9:
      push(0="err");
      trace();
      push(0="err-value");
      throw;
  };
  push(0="start");
  trace();
  try {
    label_p56:
      push(0="in-try");
      trace();
      push(0="in-catch");
      next label_p81;
  } catch(i:"e") {
  } finally {
  }
label_p81:
  trace();
  push(0=0i32, 1="f");
  callFunction();
  pop();
  push(0="in-finally");
  trace();
  push(0="end");
  trace();
  end;
