label_p0:
  push(0="before switch without break");
  trace();
  push(0="discriminant");
  getVariable();
  storeRegister(r:0);
  push(0=1i32);
  strictEquals();
  if(target=label_p90);
  push(0=r:0, 1=2i32);
  strictEquals();
  if(target=label_p99);
  next label_p108;
label_p90:
  push(0="foo");
  trace();
  next label_p99;
label_p99:
  push(0="bar");
  trace();
  next label_p108;
label_p108:
  push(0="after switch without break");
  trace();
  next label_p140;
label_p140:
  end;
