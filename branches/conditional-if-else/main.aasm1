label_p0:
  push(0="before if/else");
  trace();
  push(0="test");
  getVariable();
  not();
  if(target=label_p52);
  push(0="in if");
  trace();
  jump(target=label_p65);
  end;
label_p52:
  push(0="in else");
  trace();
  next label_p65;
label_p65:
  push(0="after if/else");
  trace();
  next label_p84;
label_p84:
  end;
