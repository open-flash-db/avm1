label_p0:
  push(0="before switch");
  trace();
  push(0="i");
  getVariable();
  storeRegister(r:0);
  push(0=0f64);
  strictEquals();
  if(target=label_p69);
  push(0=r:0, 1=1i32);
  strictEquals();
  if(target=label_p86);
  jump(target=label_p103);
  end;
label_p69:
  push(0="case 0");
  trace();
  jump(target=label_p103);
  end;
label_p86:
  push(0="case 1");
  trace();
  jump(target=label_p103);
  end;
label_p103:
  push(0="after switch");
  trace();
  next label_p121;
label_p121:
  end;
