label_p0:
  push(0="before try/catch");
  trace();
  try(catchTarget=i:"err") {
    label_p36:
      push(0="in try");
      trace();
    label_p67:
      end;
  } catch {
    label_p53:
      push(0="in catch");
      trace();
    label_p67:
      end;
  };
  push(0="after try/catch");
  trace();
label_p88:
  end;
