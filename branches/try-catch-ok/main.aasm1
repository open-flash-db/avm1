label_p0:
  push(0="before try/catch");
  trace();
  try {
    label_p36:
      push(0="in try");
      trace();
      next label_p67;
  } catch(i:"err") {
    label_p53:
      push(0="in catch");
      trace();
      next label_p67;
  }
label_p67:
  push(0="after try/catch");
  trace();
  end;
