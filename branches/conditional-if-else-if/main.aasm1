label_p0:
  push(0="before if/else(if)");
  trace();
  push(0="test");
  getVariable();
  not();
  if(target=label_p56);
  push(0="in if");
  trace();
  next label_p90;
label_p56:
  push(0="test2");
  getVariable();
  not();
  if(target=label_p90);
  push(0="in else(if)");
  trace();
label_p90:
  push(0="after if/else(if)");
  trace();
  end;
