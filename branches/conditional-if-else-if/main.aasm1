label_p0:
  push(0="before if/else(if)");
  trace();
  push(0="test");
  getVariable();
  not();
  if(target=label_p56);
  push(0="in if");
  trace();
  jump(target=label_p90);
  end;
label_p56:
  push(0="test2");
  getVariable();
  not();
  if(target=label_p90);
  push(0="in else(if)");
  trace();
  next label_p90;
label_p90:
  push(0="after if/else(if)");
  trace();
  next label_p113;
label_p113:
  end;
