label_p0:
  push(0="before try/catch/finally");
  trace();
  try {
    label_p44:
      push(0="in try");
      trace();
      next label_p75;
  } catch(i:"err") {
    label_p61:
      push(0="in catch");
      trace();
      next label_p75;
  } finally {
    label_p75:
      push(0="in finally");
      trace();
      next label_p91;
  }
label_p91:
  push(0="after try/catch/finally");
  trace();
  end;
