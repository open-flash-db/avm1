label_p0:
  constantPool(0="before nested if", 1="test", 2="in nested if", 3="after nested if");
  push(0=c:0);
  trace();
  push(0=c:1);
  getVariable();
  not();
  if(target=label_p92);
  push(0=c:1);
  getVariable();
  not();
  if(target=label_p92);
  push(0=c:2);
  trace();
  next label_p92;
label_p92:
  push(0=c:3);
  trace();
  next label_p98;
label_p98:
  end;
