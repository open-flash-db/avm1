label_p0:
  push(0="before LOGICAL AND");
  trace();
  push(0="test");
  getVariable();
  pushDuplicate();
  not();
  if(target=label_p53);
  pop();
  push(0="test2");
  getVariable();
  next label_p53;
label_p53:
  pushDuplicate();
  not();
  if(target=label_p72);
  pop();
  push(0="test3");
  getVariable();
  next label_p72;
label_p72:
  not();
  if(target=label_p100);
  push(0="in if + and");
  trace();
  next label_p119;
label_p100:
  push(0="in else + and");
  trace();
  next label_p119;
label_p119:
  push(0="after LOGICAL AND");
  trace();
  next label_p142;
label_p142:
  end;
